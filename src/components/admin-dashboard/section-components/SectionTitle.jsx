import React from "react";

const SectionTitle = ({ data }) => {
  return (
    <div style={{ color: "#FF6410", fontWeight: 700, fontSize: "30px" }}>
      {data}
    </div>
  );
};

export default SectionTitle;
